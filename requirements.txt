#Server
fastapi
uvicorn
python-multipart

# Processamento de imagens
opencv-python-headless
Pillow
numpy

# Detecção de equipamentos (Faster R-CNN)
#
# A inferência roda em CPU. No Dockerfile a instalação usa o índice
# CPU-only da PyTorch (download.pytorch.org/whl/cpu); instalar daqui pelo
# PyPI puxa as dependências CUDA e a imagem passa de 800 MB à toa.
torch
torchvision

# OCR
pytesseract
paddleocr

# Processamento e estruturação de dados
pandas

# Exportação para Excel
openpyxl

# Testes
pytest
